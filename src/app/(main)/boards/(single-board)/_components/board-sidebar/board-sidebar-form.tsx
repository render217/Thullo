"use client";

import { TBoard } from "@/types";

export default function BoardSideBarForm({
  board,
  toggleEdit,
}: {
  board: TBoard;
  toggleEdit: () => void;
}) {
  return <div>BoardSideBarForm</div>;
}
