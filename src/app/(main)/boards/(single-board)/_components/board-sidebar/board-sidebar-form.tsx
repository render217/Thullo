"use client";

import { IBoard } from "@/types";

export default function BoardSideBarForm({
  board,
  toggleEdit,
}: {
  board: IBoard;
  toggleEdit: () => void;
}) {
  return <div>BoardSideBarForm</div>;
}
